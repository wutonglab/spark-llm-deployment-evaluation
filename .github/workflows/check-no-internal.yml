name: Sanitization Lint

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check-no-internal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run sanitization scanner
        run: bash scripts/check-no-internal.sh
